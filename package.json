
{
  "name": "netpulse-isp-bridge",
  "version": "1.0.0",
  "description": "Local bridge for MikroTik API communication with CI/CD support",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 0",
    "lint": "node --check server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-routeros": "^1.2.0",
    "esbuild": "^0.24.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  }
}
